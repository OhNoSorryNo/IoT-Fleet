version: '3'
services:
  updater:
    image: updater-image
    container_name: updater
    ports:
      - "9090:9090"
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
