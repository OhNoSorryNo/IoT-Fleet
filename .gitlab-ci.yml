
stages:
  - build

build_job:
  stage: build
  script:
    - echo "Building the project..."

  artifacts:
    paths:
      - target
      - src
    expire_in: 40 week
  only:
    - main
    - develop