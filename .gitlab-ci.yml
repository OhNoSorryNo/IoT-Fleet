stages:
  - build
  - test

build_job:
  stage: build
  script:
    - echo "Building the project..."
  only:
    - main
    - develop

test_job:
  stage: test
  script:
    - ./mvnw test  # Run the unit tests
  only:
    - develop
    - main
    - 51-unit-tests
  dependencies:
    - build_job
